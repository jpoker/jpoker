'use strict';

var assert = require('chai').assert;

describe('session api', function () {

    before(function () {
    });

    after(function () {
    });

    it('dummy', function () {
        assert.equal(4, 2 * 2);
    });

});